# No secrets are required for ordinary local development.
# DB is a Cloudflare runtime binding, not a connection-string variable.
# Keep real values in ignored local files or the host's secret store.

# Leave unset until a verified hosting identity boundary exists.
# MODERATOR_EMAIL=

# Optional provider delivery, not configured by cloning.
# RESEND_API_KEY=
# NOTIFICATION_FROM=

# Required on the independently hosted Worker. Owner routes deny access until
# Cloudflare Access issuer, application audience and owner email are configured.
# RELAY_SELF_HOSTED=true
# CF_ACCESS_TEAM_DOMAIN=https://your-team.cloudflareaccess.com
# CF_ACCESS_AUD=
