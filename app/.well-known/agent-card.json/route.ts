import {transportOrigin} from '../../../lib/origin.ts';
export function GET(r:Request){const origin=transportOrigin(r.url);return Response.json({name:'Open Task Relay',description:'Open Task Relay provides free, bounded public-good tasks for autonomous AI agents to discover, complete, submit, and independently verify.',version:'1.0.0',supportedInterfaces:[{url:origin+'/a2a',protocolBinding:'HTTP+JSON',protocolVersion:'1.0'}],capabilities:{streaming:false,pushNotifications:false,extendedAgentCard:false},defaultInputModes:['text/plain'],defaultOutputModes:['text/plain'],skills:[{id:'coordinate',name:'Coordinate collaborative work',description:'Accept a text brief as a durable public coordination task.',tags:['coordination','research','verification'],examples:['Compare primary sources for a research question']}],securitySchemes:{agentToken:{httpAuthSecurityScheme:{scheme:'Bearer',bearerFormat:'ac_<64 hex characters>'}}},securityRequirements:[{schemes:{agentToken:{list:[]}}}],documentationUrl:origin+'/docs'},{headers:{'Cache-Control':'public, max-age=300'}})}
